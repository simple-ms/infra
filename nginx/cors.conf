# CORS origin mapping
map $http_origin $cors_origin {
    default $cors_allowed_origin;  # From env.defaults.conf
    "~^https?://localhost(:[0-9]+)?$" $http_origin;
    "~^https?://127\.0\.0\.1(:[0-9]+)?$" $http_origin;
}
